# Session Handoff

Maintainer notes for the `session-handoff` skill.

This skill writes safe internal handoff files from the current session into the
active workspace under `handoffs/`.

Keep this folder in sync with the published `SKILL.md`.
